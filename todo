1) user from db and return token User.ts
2) container component checkup if user is not null is shit

----
register container from
firebase input

validation error to inputsA

firebase service => not "CreateUser", yes "AddItem", "RemoveItem", etc etc
UserCrud, sería
    _collection = users
    AddUser()
        -> FirebaseService.AddItem(collection, User)


____listado de usuarios
cambiar toda la interfaz
agregar un juego
readme.md

app-listado-de-usuarios [lista]="usuarios" (usuarios es unn array q viene de otro lado)
    ngFor usuarios  pasados por parametro en html
    ->
componente usuario


(alta) (grilla usuarios)
(datos)



(input)
(actualizacion de lo q se pone en los inputs)

-------------------
lista hardcodeada // reemplazar por actualizacion de lo q se pone en los inputs